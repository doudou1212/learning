import React from "react"
import "./styles/index.scss"

const ButtonWithStyleSheet = ({className}) => <button className={`yellow-button ${className}`}>button with css</button>

export default ButtonWithStyleSheet
